package symbol

// dmECC computes Reed-Solomon error correction for data (already padded to
// s.DataCW) and returns data followed by the interleaved ECC codewords.
func dmECC(data []byte, s dmSize) []byte {
	panic("dmECC: not implemented")
}
